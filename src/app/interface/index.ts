export interface ISelect {
  selected: string;
  active: boolean;
}

export interface ICarousel {
  title: string;
  description: string;
}

export interface IFollowers {
  box: string;
  total: string;
  location: string;
}
